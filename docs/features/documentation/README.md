# Documentation

The complete offline in-app browser and its generated public mirrors.

## Articles

- [Offline documentation browser](./offline-documentation-browser.md) — Bundles canonical feature articles plus generated reviewed catalog metadata into the desktop build, renders one article at a time through safe React Markdown, and keeps links inside the browser.
